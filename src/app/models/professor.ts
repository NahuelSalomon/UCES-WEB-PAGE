export class Professor {

    id: number;
    name: string;
    ratings: number;

    constructor(id:number, name: string, ratings: number) {
        this.id = id;
        this.name = name;
        this.ratings = ratings;
    }

}
